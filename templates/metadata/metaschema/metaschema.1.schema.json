{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "http://jsonschema.net",
  "type": "object",
  "description": "Metaschema for validating the structure of the per-doctype schemas in this repository",
  "properties": {
    "mozPipelineMetadata": {
      "type": "object",
      "description": "Container for per-doctype metadata that can affect how pings are processed in the pipeline",
      "properties": {
        "bq_table": {
          "type": "string"
        },
        "bq_dataset_family": {
          "type": "string",
          "description": "The base name for the destination BigQuery dataset; if this is set to 'telemetry', the pipeline will write into 'telemetry_live'"
        },
        "expiration_policy": {
          "type": "object",
          "description": "Defines how to expire data on the destination stable table in BigQuery; if this section is absent, retention will be unlimited by default",
          "properties": {
            "action": {
              "type": "string",
              "enum": ["delete"],
              "description": "The action to take for data beyond the given retention period; currently only 'delete' is accepted, which causes a time-based partition expiration to be set on the destination stable table in BigQuery"
            }
          }
        },
        "jwe_mappings": {
          "type": "array",
          "description": "Mappings of encrypted JWE field paths to destinations where the value decrypted by the pipeline should be placed; currently only used for Account Ecosystem Telemetry; paths may indicate nested fields with dots such as 'payload.ecosystemAnonId'",
          "items": {
            "type": "object",
            "properties": {
              "source_field_path":{"type": "string"},
              "decrypted_field_path":{"type": "string"}
            }
          }
        }
      }
    }
  }
}
